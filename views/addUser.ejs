<!DOCTYPE html>
<html>
<head>
  <title>Add New User</title>
  <link href="https://fonts.googleapis.com/css?family=Roboto:400,700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/userInfo.css">
  <style>
    .form-container {
      max-width: 400px;
      margin: 40px auto;
      background: #fff;
      padding: 30px 24px 20px 24px;
      border-radius: 10px;
      box-shadow: 0 4px 24px rgba(0,0,0,0.08);
    }
    .form-container h2 {
      text-align: center;
      margin-bottom: 18px;
      color: #1976d2;
    }
    .form-group {
      margin-bottom: 18px;
    }
    .form-group label {
      display: block;
      margin-bottom: 6px;
      font-weight: 600;
      color: #333;
    }
    .form-group input {
      width: 100%;
      padding: 8px 10px;
      border: 1px solid #bdbdbd;
      border-radius: 5px;
      font-size: 15px;
      outline: none;
      transition: border 0.2s;
    }
    .form-group input:focus {
      border: 1.5px solid #1976d2;
    }
    .submit-btn {
      width: 100%;
      background: linear-gradient(90deg, #43e97b 0%, #38f9d7 100%);
      color: #fff;
      border: none;
      padding: 10px 0;
      border-radius: 6px;
      font-size: 16px;
      font-weight: 700;
      cursor: pointer;
      transition: background 0.2s;
      margin-top: 10px;
    }
    .submit-btn:hover {
      background: linear-gradient(90deg, #11998e 0%, #38ef7d 100%);
    }
    .back-link {
      display: block;
      text-align: center;
      margin-top: 18px;
      color: #1976d2;
      text-decoration: none;
      font-weight: 600;
    }
    .back-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h2>Add New User</h2>
    <form action="/addUser" method="POST">
      <div class="form-group">
        <label for="name">Name</label>
        <input required type="text" id="name" name="name" placeholder="Enter name">
      </div>
      <div class="form-group">
        <label for="class">Class</label>
        <input required type="text" id="class" name="class" placeholder="Enter class">
      </div>
      <div class="form-group">
        <label for="rollNo">Roll No</label>
        <input required type="text" id="rollNo" name="rollNo" placeholder="Enter roll number">
      </div>
      <button type="submit" class="submit-btn">Add User</button>
    </form>
    <a href="/" class="back-link">← Back to Users List</a>
  </div>
</body>